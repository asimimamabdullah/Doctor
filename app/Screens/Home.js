import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Home = () => {
	return (
		<View style={{ backgroundColor: "purple", padding: 10 }}>
			<Text style={{ flex: 1 }}>Home Component</Text>
		</View>
	);
};

export default Home;

const styles = StyleSheet.create({});
